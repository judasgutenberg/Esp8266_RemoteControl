The files in here are designed to be used by ESP8266s running the firmware of this repository in serial parsing mode, one where it looks through data in a serial stream and finds items of interest based on known sequences to look for and known offsets. Such files go in the flat LittleFS file system of the ESP8266, and, when found, are loaded into a parsing structure.
